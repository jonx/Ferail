[package]
name = "feraille-video-mpv"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "mpv (libmpv) video provider for Feraille's viewer — any format, live filters, alpha keying."

[dependencies]
feraille-core = { workspace = true }
