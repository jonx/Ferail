[package]
name = "feraille-archive"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure archive model: format identity, per-format capability matrix, and the archive table-of-contents entry model. No I/O, no platform deps — codec work lives in feraille-fs-native."

[dependencies]
feraille-core.workspace = true
