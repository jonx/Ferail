[package]
name = "feraille-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core domain types for Feraille — filesystem-agnostic, UI-agnostic"

[dependencies]
