[package]
name = "feraille-meta"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Persistent metadata store for Feraille — SQLite-backed, schema-versioned, used as the substrate for Ant Trail heat, magic cache, quarantine cache, recent folders, window/layout state, and tabs."

[dependencies]
feraille-core.workspace = true
rusqlite = { version = "0.31", features = ["bundled"] }
uuid.workspace = true
