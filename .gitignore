/target
/codex-target
.claude/settings.local.json
**/*.rs.bk
*.pdb
.DS_Store
/screenshots

# Generated filename-hazard test fixtures (names contain control/bidi chars)
test-data/filename-hazards/samples/

# Throwaway verify-step probes (Slow AI spikes) — never committed
/spikes

# Secrets and per-machine config — never commit
.env
.env.*
*.pem
*.key
.cargo/config.toml

# AROS cross-link outputs (link-aros.sh) — regenerable, the ELF is huge
crates/ferail-aros-app/build/
